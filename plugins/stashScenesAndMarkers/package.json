{
  "name": "stash-scenes-and-markers",
  "version": "0.4.1",
  "private": true,
  "description": "Stash UI plugin: unified scenes + scene markers grid",
  "scripts": {
    "build": "esbuild src/main.tsx --bundle --format=iife --outfile=stashScenesAndMarkers.js --jsx=transform --jsx-factory=React.createElement --jsx-fragment=React.Fragment",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
